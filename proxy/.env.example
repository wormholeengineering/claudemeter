# Copie para .env e preencha se necessário

# Intervalo mínimo entre scrapes do claude.ai (segundos). Padrão: 300 (5 min)
# SCRAPE_INTERVAL_SEC=300

# Saldo extra — preenchido automaticamente via cookies (prepaid/credits)
# Pode ser definido manualmente como fallback:
# CLAUDE_EXTRA_BALANCE=12.50
